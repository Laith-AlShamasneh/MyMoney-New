{
  "_comment": "DEVELOPMENT ONLY. Local-dev convenience values. Jwt:SecretKey is defined in base appsettings.json and used in every environment; a Jwt__SecretKey environment variable can still override it per-environment if desired.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5500",
      "http://127.0.0.1:5500",
      "http://localhost:3000",
      "http://localhost:4200",
      "http://localhost:8080"
    ]
  }
}
